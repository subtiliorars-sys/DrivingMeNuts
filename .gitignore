# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Secrets / env — never commit
.env
.env.*
*.pem
*.key

# Build / deps (future game code)
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/

# Scratch
*.log
tmp/
scratch/

# Local AI-tool drops (fleet ruling 2026-06-07)
.cursor/

# Dev-only contact sheet — reproducible from tools/art/build-assets.cjs.
# (The game sprites in public/generated/ ARE committed so every build path has
#  art with zero build-ordering fragility; they're tiny and reproducible.)
assets/generated/*.png

